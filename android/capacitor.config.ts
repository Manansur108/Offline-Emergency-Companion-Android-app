import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.manan.offlineai',
  appName: 'Manan Surati Offline Crisis Companion',
  webDir: 'dist'
};

export default config;
